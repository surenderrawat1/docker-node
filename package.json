{
  "name": "project-api-node",
  "version": "1.0.0",
  "description": "Fastify version of the project API using shared MySQL and Redis services.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "migrate": "node scripts/migrate.js",
    "seed:products": "node scripts/seed-products.js"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "dotenv": "^16.4.7",
    "fastify": "^5.2.1",
    "ioredis": "^5.4.2",
    "mysql2": "^3.12.0"
  },
  "devDependencies": {}
}
